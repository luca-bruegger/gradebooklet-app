import { SwissAdvancedGradesystem } from './swiss-advanced-gradesystem';

describe('SwissAdvancedGradesystem', () => {
  it('should create an instance', () => {
    expect(new SwissAdvancedGradesystem()).toBeTruthy();
  });
});
